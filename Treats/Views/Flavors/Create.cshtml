@{
  Layout = "_Layout";
}

@model Treats.Models.Flavor

<div class="jumbotron">
  <h4>Add a new Flavor </h4>
</div>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <input type="submit" class="btn btn-primary" value="Add new Flavor" />
}
<p class="btn btn-default">@Html.ActionLink("Show all categories", "Index")</p>